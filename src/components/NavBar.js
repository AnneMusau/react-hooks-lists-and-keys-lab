import React from "react";

function NavBar() {
  const links = ["home", "about", "projects"];

  return <nav>{<a href="#home">home</a>}</nav>;
  return<nav>{<a href="#about">about</a>}</nav>;
  return<nav>{<a href="#projects">projects</a>}</nav>;
}

export default NavBar;
